{
  "require-dev": {
    "phpunit/phpunit": "4.5.*",
    "apigen/apigen": "4.0.0"
  },
  "scripts": {
    "compile": "rm -fr doc && vendor/apigen/apigen/bin/apigen generate -s src -d docs --debug && vendor/phpunit/phpunit/phpunit tests",
    "commit": "./composer compile && git add -A . && git commit && git push"
  }
}
